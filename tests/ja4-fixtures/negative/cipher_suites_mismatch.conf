# Triggers mismatch_mask bit 0x0080 cipher_suites_mismatch.
# Uses cipher_mode=reorder with a cipher the lib will silently drop (TLS 1.3-only suite under tls_max=771).
# ja4-fixture-format: v1
strict=0
tls_min=771
tls_max=771
cipher_suites=4865,49195,49199
cipher_mode=reorder
alpn=h2
