CHROMIUM_BRANCH=main
# Pinned to last commit before the ML-DSA-in-TLS feature landed (2026-04-30
# 19:42:55 -0700). The next commit (a2638b6) introduced
# EVP_PKEY_generate_from_alg / EVP_pkey_ml_dsa_44 which require a newer
# BoringSSL than the one our libs/boringssl tracks. Bump this only when
# libs/boringssl is updated to a commit that exposes those symbols.
CHROMIUM_REF=bd0fdd064d09a8fedf13c78682a41f0ab2944a2c
BORINGSSL_REPLACEMENT_SRC=libs/boringssl
